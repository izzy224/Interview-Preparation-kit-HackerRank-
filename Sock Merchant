#include <bits/stdc++.h>
#include <iostream>
#include <math.h>
using namespace std;
int main(){
int pairs=0,arr[101],n,count[101] ={0};
cin>>n;
for (int i=0;i<n;i++){
    cin>>arr[i];
    count[arr[i]]++;
}
for(int i=0;i<101;i++){
    if(count[i]!=0)
    pairs=pairs+count[i]/2;
}
cout<<pairs;
}

